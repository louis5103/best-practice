# NestJS Practice 환경변수 설정 파일
# 이 파일을 복사해서 .env 파일을 만들고 실제 값으로 수정하세요.

# ===========================================
# 🚀 애플리케이션 기본 설정
# ===========================================
NODE_ENV=development
PORT=3000

# API 문서 정보
API_TITLE=NestJS Practice API
API_VERSION=1.0.0

# ===========================================
# 🐘 데이터베이스 설정 (PostgreSQL)
# ===========================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=practice_nestjs

# 개발환경에서는 자동으로 synchronize=true가 됩니다.
# 프로덕션에서는 절대 synchronize를 사용하지 마세요!

# ===========================================
# 🔴 Redis 설정 (캐싱용)
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password  # Redis에 패스워드가 설정된 경우에만 주석 해제

# ===========================================
# 🔐 JWT 인증 설정
# ===========================================
# 중요: 프로덕션에서는 반드시 강력한 비밀키를 사용하세요!
# 예: openssl rand -base64 32 명령으로 생성 가능
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Refresh Token 설정 (선택사항)
REFRESH_TOKEN_SECRET=your-refresh-token-secret
REFRESH_TOKEN_EXPIRES_IN=7d

# ===========================================
# 📧 이메일 설정 (향후 확장용)
# ===========================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=NestJS Practice <noreply@yourapp.com>

# ===========================================
# 🔍 로깅 및 모니터링 설정
# ===========================================
# 로그 레벨: error, warn, log, debug, verbose
LOG_LEVEL=debug

# Swagger UI 활성화 여부 (프로덕션에서는 false 권장)
SWAGGER_ENABLED=true

# ===========================================
# 🌐 CORS 설정
# ===========================================
# 프론트엔드 URL (쉼표로 구분하여 여러 개 가능)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ===========================================
# 💾 파일 업로드 설정 (향후 확장용)
# ===========================================
# MAX_FILE_SIZE=10485760  # 10MB in bytes
# UPLOAD_DEST=uploads/

# ===========================================
# 🔧 개발자 도구 설정
# ===========================================
# TypeScript 컴파일 옵션
TS_NODE_PROJECT=tsconfig.json

# 데이터베이스 시드 데이터 생성 여부
SEED_DATABASE=false

# ===========================================
# 📊 성능 모니터링 (선택사항)
# ===========================================
# APM_SERVER_URL=http://localhost:8200
# APM_SECRET_TOKEN=your-apm-token
# APM_SERVICE_NAME=practice-nestjs

# ===========================================
# 🔒 보안 설정
# ===========================================
# 비밀번호 해싱 라운드 (높을수록 안전하지만 느림)
BCRYPT_ROUNDS=12

# 레이트 리미팅 설정
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# ===========================================
# 📝 사용 가이드
# ===========================================
# 1. 이 파일을 .env로 복사하세요
# 2. 각 값들을 실제 환경에 맞게 수정하세요
# 3. .env 파일은 절대 git에 커밋하지 마세요!
# 4. 프로덕션에서는 환경변수를 직접 설정하거나 
#    보안이 강화된 시크릿 관리 서비스를 사용하세요

# 🚨 보안 주의사항:
# - JWT_SECRET은 반드시 복잡한 값으로 변경하세요
# - 데이터베이스 비밀번호는 강력하게 설정하세요  
# - 프로덕션에서는 DEFAULT 값들을 절대 사용하지 마세요
# - .env 파일을 실수로 공개 저장소에 올리지 않도록 주의하세요
